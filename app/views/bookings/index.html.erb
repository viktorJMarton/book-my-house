<h1>Book-My-House</h1>
<p><%= link_to 'Book the house!', new_booking_path %></p>
<p>
  ...or check whether the house is booked already
  <%= link_to 'here!', new_query_path %>
</p>

<p>
  ...or check the statistics page
  <%= link_to 'here!', statistic_path %>
</p>

<h2>The full list of bookings:</h2>

<div class='bookings'>
  <% if @bookings.any? %>
    <%= render partial: 'bookings/booking', collection: @bookings %>
  <% else %>
    There is no booking yet.
  <% end %>
</div>


<div class="pagination-options">
  <%= form_tag bookings_path, method: :get do %>
    <%= select_tag :per_page, options_for_select([4, 6, 8, 12, 20], params[:per_page]), onchange: "this.form.submit()" %>
  <% end %>
</div>

<div class="flickr_pagination">
  
    <%= will_paginate container: true %>
  </div>